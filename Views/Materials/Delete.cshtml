@model printCostTracker.Models.Material

@{
    ViewData["Title"] = "Delete Material";
}

<h1>Delete Material</h1>

<div class="alert alert-danger">
    <h3>Are you sure you want to delete this material?</h3>
</div>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Name</dt>
        <dd class="col-sm-10">@Model.Name</dd>

        <dt class="col-sm-2">Description</dt>
        <dd class="col-sm-10">@(string.IsNullOrEmpty(Model.Description) ? "-" : Model.Description)</dd>

        <dt class="col-sm-2">Type</dt>
        <dd class="col-sm-10">@((MaterialType)Model.MaterialType)</dd>

        <dt class="col-sm-2">Color</dt>
        <dd class="col-sm-10">@(string.IsNullOrEmpty(Model.Color) ? "-" : Model.Color)</dd>

        <dt class="col-sm-2">Cost per Gram</dt>
        <dd class="col-sm-10">$@Model.CostPerGram.ToString("F3")</dd>

        <dt class="col-sm-2">Density</dt>
        <dd class="col-sm-10">@Model.Density.ToString("F2") g/cm³</dd>

        <dt class="col-sm-2">Status</dt>
        <dd class="col-sm-10">
            @if (Model.IsActive)
            {
                <span class="badge bg-success">Active</span>
            }
            else
            {
                <span class="badge bg-secondary">Inactive</span>
            }
        </dd>

        <dt class="col-sm-2">Created</dt>
        <dd class="col-sm-10">@Model.CreatedAt.ToString("MM/dd/yyyy HH:mm")</dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-danger" />
        <a asp-action="Index" class="btn btn-secondary">Back to List</a>
    </form>
</div> 